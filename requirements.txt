# Core ML / serving stack (Python 3.11; TF has no wheels for 3.14).
# TF 2.15 drops TF 2.13's typing-extensions<4.6 cap (which clashed with
# gradio/fastapi/pydantic v2) and still ships Keras 2, required by transformers.
tensorflow==2.15.0
transformers==4.35.0
huggingface_hub==0.17.3
sentencepiece==0.1.99
numpy==1.24.3

# Web app + API
gradio==4.7.1
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Data prep + evaluation
datasets==2.14.6
# datasets 2.14 uses pyarrow.PyExtensionType, removed in pyarrow >= 15; pin to 14.x.
pyarrow==14.0.2
pandas==2.0.3
scikit-learn==1.3.2
openpyxl==3.1.2
requests==2.31.0
